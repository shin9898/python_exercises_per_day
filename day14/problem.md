### Day14: 問題

```python
"""
【問題】
線形的なN個の数列Aがある。

Aに対して次の操作を行う。

- 各要素 A[i] を、「左右の位置に存在する要素との差の総和」に置き換える
  - すなわち、既存のA[i]を使わない。
  - 編集するための新しい配列を用意してよい

例)
入力: A = [3, 1, 4]
操作:
- 0番目: (1-3) + (4-3) = 1 - 3 + 4 - 3 = -1
- 1番目: (3-1) + (4-1) = 3 - 1 + 4 - 1 = 5
- 2番目: (3-4) + (1-4) = 3 - 4 + 1 - 4 = -4

出力: [-1, 5, -4]

【条件】
- 1 ≤ N ≤ 100
- -100 ≤ A[i] ≤ 100

【ヒント】
- A[i]以外の要素すべてとの差を総和すると思ってもらえればシンプル
- 1回では計算しにくいと思うので、先に"Aの総和"を求めてから、各A[i]からパターンを消去した方が楽
"""

# テスト用
if __name__ == '__main__':
    A1 = [3, 1, 4]
    # 期待出力: [-1, 5, -4]
    A2 = [2, 2, 2]
    # 期待出力: [0, 0, 0]
    A3 = [5, -5]
    # 期待出力: [-20, 20]
    print()
    print()
    print()
```
